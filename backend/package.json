{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for an online tester",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "npx tsx watch --include './src/**/*.ts' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aayush Batwara",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "groq-sdk": "^0.8.0"
  }
}
